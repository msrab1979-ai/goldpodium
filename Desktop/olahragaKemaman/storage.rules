rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Logos — superadmin boleh tulis, semua boleh baca
    match /logos/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
